# hyperuranion-research

Research codebase for hyperuranion experiments.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Main scripts

- `train.py`: model training entrypoint
- `inference.py`: inference entrypoint
- `eval_rd.py`: rate-distortion style evaluation utilities
